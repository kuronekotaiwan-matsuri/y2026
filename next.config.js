/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/y2026',
  assetPrefix: '/y2026',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
